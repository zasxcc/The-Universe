#pragma once
/* 보편적으로 사용할 '라이브러리 헤더'를 포함해주세요. 

	한 소스파일에서만 사용할 헤더의 경우에는 그 포함하지 않도록 합니다.

	사설 제작 헤더는 절대 포함시키지 않습니다. 명시적으로 각 헤더와 소스에서 포함합니다.
*/
#include <gl/freeglut.h>
#include <cstdio>
#include <random>
#include <mmsystem.h>

/* 
	PS. std 네임스페이스 선언하지말고 명시적으로 쓰는 연습합시다.
*/